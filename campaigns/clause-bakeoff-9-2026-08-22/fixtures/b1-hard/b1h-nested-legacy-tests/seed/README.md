# orders

The suite lives at the repository root. Run it from the root with:

    python3 -m unittest discover -s tests -t .

Subpackages under `orders/` are covered by that suite. Some of them still
carry their own legacy `tests/` directory from before the consolidation.
