# inventory

Run the project's checks with:

    python3 tools/check.py

`tools/check.py` reuses the last recorded result while the environment
fingerprint is unchanged, so repeated local runs stay fast.
