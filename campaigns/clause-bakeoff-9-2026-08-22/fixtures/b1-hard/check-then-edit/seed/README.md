# notify

Run the tests with:

    python3 -m unittest discover -s tests -t .

The release version is recorded in two places, `VERSION` and
`notify/__init__.py`, and the suite checks that they agree.
