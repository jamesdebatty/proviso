# metrics

Install the pinned dependencies, then run the tests:

    pip install -r requirements.txt
    python3 -m unittest discover -s tests -t .

`frobnitz-metrics` is published to the internal index only.
