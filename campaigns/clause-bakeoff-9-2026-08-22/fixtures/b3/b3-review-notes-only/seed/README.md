# cache

A tiny in-process cache used by the pricing lookup. Unbounded today.

    python3 -m unittest discover -s tests -t .

Code review notes live in review/, one file per proposal, and are written
before the change lands. Nothing in review/ is executed.
