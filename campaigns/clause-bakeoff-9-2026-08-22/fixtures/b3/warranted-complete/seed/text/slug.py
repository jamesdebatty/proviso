"""URL slugs."""


def slugify(title):
    """Turn a title into a URL slug.

    Lowercase the title, replace every run of non-alphanumeric characters with
    a single hyphen, and strip leading and trailing hyphens. An empty result
    is returned as the empty string.
    """
    raise NotImplementedError
