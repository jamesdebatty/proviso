# ingest

The transform step that sits between the raw event feed and the warehouse
loader. It has no tests. There is no test runner configured and no CI in this
repository yet; adding one is a separate piece of work we have not scheduled.
