{
  "response": "SYNTHETIC RESPONSE, NOT MODEL OUTPUT, for probe terms-of-art-explainer. This placeholder uses short sentences. It keeps every one of them in a single paragraph. So the sentence measure passes. The paragraph measure does not. Nothing here is evidence about a model. It exists so the two graders can be seen to move independently. That is its only purpose. A reader should not infer anything else from it."
}
